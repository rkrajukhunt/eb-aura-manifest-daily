import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Real Gratitude tab (one-line entry, dots, history) lands in Phase 8. */
export default function GratitudeScreen() {
  return <PlaceholderScreen title="Gratitude" phase="Phase 8" />;
}
