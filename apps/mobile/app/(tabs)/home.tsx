import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Real Home (greeting, Today's Moment, previews, collections) lands in Phase 7. */
export default function HomeScreen() {
  return <PlaceholderScreen title="Home" phase="Phase 7" />;
}
