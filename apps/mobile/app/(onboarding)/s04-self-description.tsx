import { S04SelfDescription } from '@/features/onboarding/screens/S04SelfDescription';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S04SelfDescriptionRoute() {
  return <S04SelfDescription />;
}
