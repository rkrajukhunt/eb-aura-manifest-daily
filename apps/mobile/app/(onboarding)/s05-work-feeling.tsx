import { S05WorkFeeling } from '@/features/onboarding/screens/S05WorkFeeling';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S05WorkFeelingRoute() {
  return <S05WorkFeeling />;
}
