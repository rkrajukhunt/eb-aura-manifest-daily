import { S08DreamCity } from '@/features/onboarding/screens/S08DreamCity';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S08DreamCityRoute() {
  return <S08DreamCity />;
}
