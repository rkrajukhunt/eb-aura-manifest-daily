import { S07DreamHome } from '@/features/onboarding/screens/S07DreamHome';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S07DreamHomeRoute() {
  return <S07DreamHome />;
}
