import { S11ArrivalTime } from '@/features/onboarding/screens/S11ArrivalTime';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S11ArrivalTimeRoute() {
  return <S11ArrivalTime />;
}
