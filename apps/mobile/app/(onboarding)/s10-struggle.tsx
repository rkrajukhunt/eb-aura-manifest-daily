import { S10Struggle } from '@/features/onboarding/screens/S10Struggle';

/** Thin route (01 §2) — the feature owns the logic. */
export default function S10StruggleRoute() {
  return <S10Struggle />;
}
