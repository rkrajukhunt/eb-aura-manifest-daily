import { WhatAuraKnows } from '@/features/memory/WhatAuraKnows';

/** Push route from Profile (06 §1). Thin — the feature owns the logic (01 §2). */
export default function WhatAuraKnowsRoute() {
  return <WhatAuraKnows />;
}
