/**
 * Boot copy (05 §8, §9). One honest line — never a spinner, never a code.
 */
export const bootCopy = {
  /** Offline first launch, retries exhausted (03 §2 edge cases). */
  cantReach: 'I can’t reach you right now. I’ll keep trying.',
} as const;
